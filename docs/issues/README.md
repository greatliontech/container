# Issues

Tracked deferrals carrying a `Lands:` trigger. On resolution, the
load-bearing rationale is promoted into a kept-current artifact and the
issue file deleted — git holds history.

| Issue | Summary | Lands |
|---|---|---|
| [sandbox-create-path-replica](sandbox-create-path-replica.md) | sandbox maintains an independent replica of the create path; kernel-rule fixes apply to both | sandbox's first tagged release delivering its spec's Strong row whole |
